---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('it-IT', {
			timeZone: 'UTC',
				year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
