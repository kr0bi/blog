---
import SiteLayout from '../layouts/SiteLayout.astro';
import { withBase } from '../lib/paths';
---
<SiteLayout title="Page not found" noindex>
  <header class="page-intro"><p class="eyebrow accent">404 · A small detour</p><h1>A little off the path.</h1><p>This page isn’t here. There’s more to discover back at the journal.</p><a class="text-link" href={withBase()}>Back home ↗</a></header>
</SiteLayout>
